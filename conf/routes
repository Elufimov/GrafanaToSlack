# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
POST    /render                              controllers.Application.insert(panelId: Int, slug: String, from: String, to: String, width: Int, height: Int)
GET     /image/:uuid                         controllers.Application.image(uuid: UUID)


    