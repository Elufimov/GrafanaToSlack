# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /image/:uuid                         controllers.Application.image(uuid: UUID)
POST    /forward                            controllers.Application.forward()

    